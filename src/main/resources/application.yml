spring:
    application:
        name: GestorDocumentalBackEnd
    datasource:
        url: jdbc:mysql://localhost:3306/gestor_documental?CreateDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
        username: root
        password: infinity
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        database: mysql
        show-sql: false
        generate-ddl: true
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                format_sql: true
        open-in-view: true